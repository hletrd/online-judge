FROM alpine:3.21

RUN apk add --no-cache ocaml ocaml-findlib musl-dev && \
    adduser -D -s /bin/false judge && \
    mkdir -p /workspace && \
    chown judge:judge /workspace

WORKDIR /workspace
USER judge
