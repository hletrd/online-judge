# Document Specialist Review — RPF Cycle 7/100

**Date:** 2026-04-26
**Cycle:** 7/100
**Lens:** documentation accuracy, code-vs-doc consistency, cross-references, AGENTS.md / CLAUDE.md / README.md / .env.example coverage

---

## Cycle-6 carry-over verification

Cycle-6 plan tasks confirmed:
- Task A: deploy-docker.sh Step 5b backfill comment block (lines 544-569) cross-references cycle-6 plan + cycle-5 aggregate. ✓
- Task B: AGENTS.md "Database migration recovery (DRIZZLE_PUSH_FORCE)" section + .env.example commented entries. ✓
- Task C: cycle-5 plan archived. ✓

---

## DOC7-1: [LOW, NEW] AGENTS.md "Database migration recovery (DRIZZLE_PUSH_FORCE)" section at line 349 does NOT cross-reference the cycle-6 plan or aggregate

**Severity:** LOW (cross-reference completeness)
**Confidence:** HIGH

**Evidence:**
- `AGENTS.md:349-365` (the new "Database migration recovery" section): describes when to use `DRIZZLE_PUSH_FORCE=1`, what it does, and notes the Step 5b backfill runs unconditionally.
- Does NOT link to:
  - `plans/done/2026-04-26-rpf-cycle-6-review-remediation.md` (the plan)
  - `plans/done/2026-04-26-rpf-cycle-5-review-remediation.md` (the predecessor)
  - `.context/reviews/_aggregate-cycle-5.md` AGG5-1 (the original failure mode)

**Why it's worth tracking:** Operators who hit the deploy warn want to find the FULL context. AGENTS.md gives them the WHAT and WHY at a high level, but the deep dive is in the plans.

**Fix:** Add a "See also" line at the end of the AGENTS.md section.

**Exit criteria:** AGENTS.md section cross-references the source plans.

**Carried-deferred status:** Defer (cosmetic doc improvement).

---

## DOC7-2: [LOW, NEW] `.env.example` and `.env.production.example` reference `DRIZZLE_PUSH_FORCE` but the inline comment uses section-name reference (string-search) not anchor

**Severity:** LOW (cross-reference precision)
**Confidence:** MEDIUM

**Evidence:**
- `.env.example:25-31` and `.env.production.example:14-20`: comment block references "AGENTS.md \"Database migration recovery (DRIZZLE_PUSH_FORCE)\" section".
- The cross-reference is by section name, not by line/anchor. Works for human operators but is fragile to AGENTS.md re-titling.

**Fix:** Use a stable anchor or keep section-name reference. Current ref is acceptable.

**Exit criteria:** Doc references survive a section rename.

**Carried-deferred status:** Defer (current ref works; precision improvement).

---

## DOC7-3: [LOW, NEW] `0021_lethal_black_tom.sql` filename is auto-generated by drizzle-kit (no operator intent) — same finding as cycle-6 CR6-3

**Severity:** LOW (operator readability — same as cycle-6 carried-deferred CR6-3)
**Confidence:** HIGH

**Evidence:** Identical to cycle-6 CR6-3.

**Carried-deferred status:** Defer per cycle-6 reasoning.

---

## DOC7-4: [LOW, NEW] Cycle-6 plan deferred-table column "exit criterion to re-open" is well-formed but uses inconsistent phrasing

**Severity:** LOW (process consistency)
**Confidence:** HIGH

**Evidence:**
- `plans/open/2026-04-26-rpf-cycle-6-review-remediation.md` deferred table: 17 entries; ~40% use "X is added", ~30% use "Pick up opportunistically", ~30% use other phrasing.

**Fix (low):** Standardize on three exit-criterion shapes (event-based / time-based / opportunistic).

**Exit criteria:** Future RPF plan deferred-tables follow the standard shape.

**Carried-deferred status:** Defer (process improvement).

---

## DOC7-5: [LOW, NEW] CLAUDE.md letter-spacing rule for Korean — manual enforcement; cycle-7 verification confirms compliance

**Severity:** LOW (enforcement — manual policy)
**Confidence:** HIGH

**Evidence:**
- `CLAUDE.md` rule: "Keep Korean text at the browser/font default letter spacing. Do not apply custom letter-spacing (or tracking-* Tailwind utilities) to Korean content."
- Verified by grep: no new `tracking-*` utilities introduced this cycle. ✓ Compliant.

**Fix:** No action — current state compliant. Recording the verification.

**Carried-deferred status:** Resolved (cycle-7 verification confirms compliance).

---

## DOC7-6: [LOW, NEW] `.context/reviews/README.md` does not mention the per-cycle review file convention (architect.md, code-reviewer.md, etc.)

**Severity:** LOW (process doc — convention is implicit)
**Confidence:** HIGH

**Evidence:**
- `.context/reviews/` contains 80+ files including `architect.md`, `code-reviewer.md`, etc. (per-cycle review artifacts).
- `.context/README.md` mentions "Reviews (Active)" listing specific named files but does NOT document the per-agent-per-cycle convention.

**Fix:** Add a section to `.context/README.md` describing the per-agent review file convention used by RPF cycles.

**Exit criteria:** Convention is documented.

**Carried-deferred status:** Defer (process doc, not blocking).

---

## Summary

**Cycle-7 NEW findings:** 0 HIGH, 0 MEDIUM, 6 LOW (all carried-deferable; DOC7-5 resolved at verification).
**Cycle-6 carry-over status:** All cycle-6 documentation tasks remain in place. AGENTS.md section is well-formed; cross-references could be tighter.
**Doc verdict:** No documentation gaps that block operators. The recent cycle-6 docs are accurate and present.
